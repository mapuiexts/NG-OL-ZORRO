import { MapControlDirective } from './map-control.directive';

describe('MapControlDirective', () => {
  it('should create an instance', () => {
    const directive = new MapControlDirective();
    expect(directive).toBeTruthy();
  });
});
